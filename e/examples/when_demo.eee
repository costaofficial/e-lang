// when — condition demo
do
    find all ".product"
    when count > 5 do
        log "many products"
    done

    when count <= 3 do
        log "few products"
    done

    get number from "#price"
    when number > 100 do
        log "expensive"
    done

    when item visible do
        log "item is visible"
    done
done
